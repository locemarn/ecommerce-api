{
  "kind": "collectionType",
  "collectionName": "publishers",
  "info": {
    "name": "publisher"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "name": {
      "type": "string",
      "unique": true,
      "required": true
    },
    "slug": {
      "type": "uid",
      "targetField": "name",
      "required": true
    },
    "products": {
      "via": "publisher",
      "collection": "product"
    }
  }
}
